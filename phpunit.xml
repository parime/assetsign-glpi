<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         failOnWarning="true"
         failOnDeprecation="false">
    <testsuites>
        <testsuite name="remise">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source>
        <!-- front/ exclu : controleurs HTTP exerces manuellement (session/requete reelle),
             pas par la suite PHPUnit (boot direct du noyau, cf. tests/bootstrap.php) - les
             inclure ferait remonter un 0% de couverture trompeur, pas un manque de tests. -->
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
